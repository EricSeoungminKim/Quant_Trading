# secrets
.env
.env.local
*.pem

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# runtime state & caches (never commit market data or positions)
# 루트 한정 — quant/adapters/data/ 는 소스 코드다(옛 이름 quant_engine/data/)
/data/
# 노트북 실험이 만드는 런타임 상태. 루트 앵커(/data/)가 이 경로를 못 덮어
# backtest/data/state/portfolio.json 이 실제로 커밋돼 있었다(2026-08-26 정리).
backtest/data/

# os / editor
.DS_Store
.idea/
.vscode/

# omc runtime state
.omc/

# graphify 지식 그래프 — 재생성 가능한 산출물만 제외한다.
# docs/vault/*.md (손으로 쓴 기억 노트)는 추적한다 — 그게 AI의 영속 메모리다.
graphify-out/
docs/vault/graph/
data/backups/
out/
