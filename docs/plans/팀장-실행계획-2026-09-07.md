# 팀장 실행계획 (2026-09-07 저녁, 소유자 위임)

소유자: "너가 이 프로젝트의 팀장이고 나는 최초 고객이다. 추천 방향으로 전부 진행하고 기록하라 —
순차적으로 검증하며." 아래는 그 위임 아래 내가 내린 결정과 집도 순서다. 결정마다 근거(실측)와
검증 방법을 적고, 실행 결과는 `docs/vault/변경기록.md`에 시각 순으로 남긴다.

## 결정 (보고서 ⑤~⑧)

| # | 질문 | 결정 | 근거 | 검증 |
|---|---|---|---|---|
| ⑥ | 과거 원장을 판본별 소급 태깅할지, 판정 불가로 리셋할지 | **둘 다, 역할을 나눠서.** 소급 근사는 *분석용 사이드카*(approx=true)로만 만들고, *판정*(스코어보드·governor·자본 심사)은 오늘 밤부터 찍히는 정확한 지문 기준 "현재 판본만"으로 한다 → 현재 판본 표본 <30 인 레인은 정직하게 "판정 불가" | `docs/research/params-version-retrotag-2026-09-07.md`: scalp_1m 6판본, 현재 2트립. 근사 지문을 판정에 쓰면 오염을 다른 오염으로 바꾸는 것 | `ledger-versions --dry-run` 표 = 연구 메모 표, 스코어보드에 "현재 판본 n / 전체 N" 표시, `judge_current_params_only: true` 후 governor 제안 0건(표본 부족) 확인 |
| ⑦ | NO_GO 를 뒤집어 재활성한 전략에 판정일 강제 | **강제한다.** letf_pair_qqq/sox·intraday_momentum 에 `validation.review_by: 2026-10-06`(적립 3레인과 같은 한 달 창) + evidence 문구. 설정 린트 테스트: evidence 에 NO_GO 가 있으면 review_by 필수 | 판정 철회가 아닌 관찰이라면 끝나는 날이 코드에 있어야 한다(적립 3레인 선례) | 테스트 + 08:00 알림 크론(lane_review_reminder) 드라이런 |
| ⑧ | 자동 판정에 다중검정 보정 | **자동 경로에만 넣는다.** allocator 강등·governor 킬스위치는 같은 실행에서 후보 전체에 Benjamini-Hochberg(q=0.10)를 적용해 통과한 것만 자동 반영; 사람이 보는 리포트 수치는 보정 없이 그대로(정보 손실 방지) | quant-expert §2 "다중검정 편향 무시"는 금기인데 코드엔 0건; 자동으로 자본을 줄이는 경로가 이미 있다 | 합성 17전략 중 1개만 진짜 음수인 케이스에서 보정 전 3건 → 보정 후 1건 강등 테스트 |
| ⑤ | 스캘핑 규칙 방향 | **(a) ADX 추세 게이트를 scalp_1m(기본 갈래)에서만 실제 차단으로 켠다 — A/B**(scalp_1m_cat 은 기록만 유지). (b) W%R 게이트는 켜지 않는다(역효과 실측). (c) MA60 트레일 교체·갭업일 진입 제한은 **백테스트 GO 전엔 라이브 변경 없음** — 연구 과제로 KR 1분봉 레이크에서 walk-forward | 원장 63트립: 게이트 통과 +38.6bp vs 차단후보 +4.0bp; 오늘 차단후보 5건 중 4건 손실; W%R 통과 9건 −24bp | 파라미터 변경 = 새 지문 → 내일부터 판본 깨끗한 A/B. 2주 후 `ab_compare --current-params-only` |

## 집도 순서 (각 단계: 근거 → 테스트 → 배포 확인 → 기록)

1. **판본 지문**(W-FP): Fill.params_fingerprint + 원장 schema=2, 조립·리로드 시 지문 맵, `ledger-versions` 사이드카 도구, 스코어보드 판본 필터, allocator/ledger 표본 기준 30 통일. → 검증 후 커밋.
2. **⑦ 설정**: review_by 3레인 + 린트 테스트. **⑤(a) 설정**: `scalp_1m.trend_gate_mode: enforce`. (설정은 핫리로드 대상이라 엔진 무접촉으로 즉시 반영되지만, 지문 스탬프와 함께 밤 재시작 1회로 정리.)
3. **⑧ 보정**: allocator/governor 자동 경로에 BH. 테스트. ✅ 521ce5d (2026-09-07 20:0x)
4. **⑥ 운영**: EC2 에서 `ledger-versions --write-sidecar`(원장 불변), `governor.judge_current_params_only: true`.
5. **밤 플랫 창 재시작** (V2 거래 평면 수정 + 지문 스탬프 반영) → 기동 로그·대사·하트비트 확인.
6. **연구 과제 발주**(quant-backtest): scalp_1m 청산 규칙(MA60 트레일 vs ATR 트레일 vs 시간 손절) × ADX 게이트 on/off × 갭업일 진입 제한 — KR 1분봉 레이크, walk-forward, 비용 KR 28bp, 사전 등록. GO 만 라이브. ✅ R-SCALP(청산 3가설)·R-SCALP2(진입 5가설 10변형) 완료 — **17변형 전부 NO_GO**(`docs/research/scalp-{exit,entry}-study-2026-09-07-SUMMARY.md`). 결정: 라이브 규칙 변경 없음, ⑤(a) 게이트 차단은 이미 철회(4f71c7d). scalp_1m/scalp_1m_cat 은 소유자 차트 리뷰용 관찰 레인으로만 2026-10-06 까지 유지, 그날 소유자 규칙 아이디어의 GO 가 없으면 비활성 권고.
7. **운영 잔여**: ops_judge → Claude 경로, daily_wrap 발송 게이트 경유, 16:00~16:25 배치 간격 재배치(V3 표 기준), 아카이브 정리 규칙(*.pre-epoch-*). ✅ 34823e8 (2026-09-07 20:1x) — 6번 연구도 완료

## 보고 규칙 (최초 고객에게)

- 매일 아침 보고서(같은 링크) 갱신: 무엇을 바꿨고(왜, 근거), 무엇이 확인됐고, 무엇이 판정 불가인지.
- 규칙 변경은 반드시 A/B 또는 백테스트 GO 를 거치고, 라이브 반영 시각과 지문을 기록한다.
- 사장님이 직접 볼 것: 데일리 매매 리뷰(KR/US WRAP 7절) — 규칙 방향에 대한 의견은 언제든 반영.
