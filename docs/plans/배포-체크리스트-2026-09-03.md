# 배포 체크리스트 — 2026-09-03 (KR 마감 후)

세션 2026-09-02~03 의 변경(엔진 결함 7건, 원장/보고 7건, 뉴스 창, 로스터 단타
전용화, manual_recs, 시총·섹터, 리스크 레일, 백테스트 인프라, 신규 전략 3종
`enabled: false`)을 한 번에 올린다. 배경은 `docs/vault/변경기록.md` 최상단 3항목.

## 배포 창
- **15:45 ~ 20:30 KST**. 15:35 session_pnl · 15:40 manual_recs(신규 크론) 이후,
  scalp_1m US 프리마켓(21:00 KST) 전. 엔진 재시작은 무포지션 상태에서.
- 확인: `ssh ubuntu@100.87.129.113 "cd quant_trading_kiwoom && cat data/state/portfolio.json | python3 -c 'import json,sys; p=json.load(sys.stdin); print({k:v for k,v in p.get(\"positions\",{}).items() if v.get(\"qty\")})'"` 가 빈 dict.

## 순서
1. 로컬: `git status` 클린, `uv run pytest -q` 통과(2026-09-03 06:xx 5,574 passed).
2. `QT_SSH_HOST=ubuntu@100.87.129.113 make deploy` — git pull, uv sync,
   `crontab server/crontab.txt`(manual_recs KR 15:40/US 06:30 포함), 엔진·브리지 재시작.
3. deploy.sh 가 **하지 않는** 것(수동):
   - 텔레그램 30분 수집기: `sudo cp server/systemd/telegram-collect.{service,timer} /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now telegram-collect.timer`
   - journald 상한: `sudo mkdir -p /etc/systemd/journald.conf.d && sudo cp server/systemd/journald.conf.d/quant.conf /etc/systemd/journald.conf.d/ && sudo systemctl restart systemd-journald` (엔진 무관, 장중도 안전)
4. 스모크(재시작 후 5분):
   - `sudo journalctl -u quant-engine -n 200 --no-pager | grep -cE " ERROR "` = 0
   - 부팅 로그에 `capital_policy declared` 배분표, `universe_filter` 유지/제외 줄, `require_books_capable_broker` 통과.
   - `./server/scripts/manual_recs.sh KR` 을 `DRY_RUN=1` 로 1회 → 메시지 형식 확인.
   - `.venv/bin/python -m quant.apps.cli scoreboard --ab` 가 판단 불가 줄을 찍는지.
5. 22:10 US 유니버스 롤 이후: 닫힌 KR 심볼 경고가 시간당 1만 줄 → 0 인지
   (`journalctl --since "-10min" | grep -c "아직 수신된 틱 없음"`).

## 배포 직후(마감 후 3시간) — 키움 1년 분봉 백필
- 실측: `ka10080` 1m/5m 모두 2025-09-01 까지(정확히 1년), 종목당 ~2분, 1 req/s.
- EC2 에서만 가능(지정단말기). 장중 금지(엔진 키움 쿼터 공유).
- 절차: `quant-backtest/qb/fetch_kiwoom.py` 를 EC2 `/tmp/qb_probe/qb` 로 복사,
  `/tmp/Quant_Trading -> quant_trading_kiwoom` 심링크, `.env.local` 로드 후
  `fk.fetch(symbols=<KR 유니버스 ~90>, interval="1m")` → 산출 lake 를 로컬
  `quant-backtest/data/lake/` 로 rsync → `notebooks/03_kr_walkforward` 재실행 →
  `backtest-gate` 로 GO/NO_GO.

## 롤백
- `git revert` 후 `make deploy`. settings 만 되돌릴 땐 `config/settings.yaml`
  핫리로드(다음 유니버스 롤)로 충분.
